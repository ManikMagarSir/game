# @react-three/fiber 9.7 declares peer react ">=19 <19.3" while Vite scaffolds
# react 19.3+. The combination works at runtime; relax peer resolution so a
# plain `npm install` succeeds for everyone.
legacy-peer-deps=true
